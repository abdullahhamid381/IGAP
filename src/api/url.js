const urls = {
  // baseUrl: "http://localhost:5000",
  baseUrl: "https://igap.vercel.app",
  auth: {
    login: "/auth/login",
    signup: "/auth/signup",
    refreshAccessToken: "/auth/RefreshAccessToken",
  }
};

export default urls;
